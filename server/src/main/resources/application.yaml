spring:
  application:
    name: coffee-shop-server

  datasource:
    url: jdbc:postgresql://capstone-do-user-4787316-0.j.db.ondigitalocean.com:25060/coffee2352

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: ${FLYWAY_ENABLED:false}

  profiles:
    active:
      - local

server:
  error:
    include-message: never
